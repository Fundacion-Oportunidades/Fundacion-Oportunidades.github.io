---
import Mission from "./Mission.astro";
import valuesHowToApply from "../data/valuesHowToApply";
---

{
  valuesHowToApply.map((p) => (
    <Mission title={p.title} paragraphs={p.paragraphs} />
  ))
}
