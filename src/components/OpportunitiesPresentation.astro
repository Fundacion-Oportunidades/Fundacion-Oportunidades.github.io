---
import valuesPresentationNetworking from "../data/valuesPresentationNetworking";
import Mission from "../components/Mission.astro";
---

{
  valuesPresentationNetworking.map((i) => (
    <Mission title={i.title} paragraphs={i.paragraphs} />
  ))
}
