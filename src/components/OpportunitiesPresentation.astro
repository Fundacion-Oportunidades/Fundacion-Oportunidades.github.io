---
import type { TextCardItems } from "../types/types";
import valuesPresentationNetworking from "../data/valuesPresentationNetworking";

import TextCard from "./TextCard.astro";
---

{
  valuesPresentationNetworking.map((i) => (
    <TextCard title={i.title} paragraphs={i.paragraphs} />
  ))
}
