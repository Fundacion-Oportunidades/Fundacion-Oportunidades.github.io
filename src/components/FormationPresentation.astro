---
import Mission from "./Mission.astro";
import formationPresentation from "../data/valuesFormationPresentation";
---

{
  formationPresentation.map((p) => (
    <Mission title={p.title} paragraphs={p.paragraphs} />
  ))
}
